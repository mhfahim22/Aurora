import MrCode
output("-- MHA forward-only test --\n")
m = model_create("sequential")
add(m, dense(4, "relu"))
add(m, mha(2))
add(m, dense(1, "sigmoid"))
d = csv("test.csv")
output("forward pass...\n")
p = predict(m, d)
output(p)
output("\ndone\n")
