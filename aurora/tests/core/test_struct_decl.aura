# ── Struct Declaration & Usage ──
struct Point
    x = 0
    y = 0

p = Point { x: 10, y: 20 }
output(p.x)
output(p.y)

# ── Nested struct ──
struct Circle
    cx = 0
    cy = 0
    radius = 0

c = Circle { cx: 1, cy: 2, radius: 5 }
output(c.cx)
output(c.cy)
output(c.radius)

output(111)
