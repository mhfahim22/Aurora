# Lexer keyword tests

function test_keywords()
    if true
        output(1)
    else
        output(0)
    while false
        output(0)
    for i in 1
        output(1)
    return 0

output(0)
test_keywords()
output("LEXER_KEYWORDS_OK")
