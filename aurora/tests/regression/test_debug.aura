import "test"

lambda throwing_fn():
    throw "error"
end

function main()
    test_begin("Debug")
    output("about to call assert_throws")
    assert_throws(throwing_fn)
    output("after assert_throws")
    test_pass("basic")
    test_report()
end
