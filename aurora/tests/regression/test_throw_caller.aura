lambda throwing_fn():
    throw "error"
end

function caller()
    throwing_fn()
end

function main()
    output("about to try")
    did_catch = false
    try:
        caller()
    catch e:
        did_catch = true
    end
    if did_catch
        output("caught it")
    else
        output("NOT caught")
    end
end
