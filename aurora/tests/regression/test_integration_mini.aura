// import "test" — module may not exist; test functions may be builtins
// import "mock" — module may not exist; mock functions may be builtins

function main()
    test_begin("Integration Tests")

    lambda throwing_fn():
        throw "error"
    end
    assert_throws(throwing_fn)
    test_pass("assert_throws")

    test_report()
end
