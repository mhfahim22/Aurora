======================================================================
              AURORA LANGUAGE DEVELOPMENT ROADMAP (AI-OPTIMIZED)
======================================================================
[Claimed Version: 1.0.0] | [Base: C++17/23 & LLVM] | [Status: Pre-Alpha]
======================================================================

----------------------------------------------------------------------
PHASE 1: RUNTIME CORE & CRITICAL FIXES (PRIORITY 1)
----------------------------------------------------------------------
[x] TASK 1.1: Fix Fiber Implementation (Async Engine)
    - Description: Current fiber implementation is synchronous; 'yield' is a no-op.
    - AI Prompt Target: Modify 'runtime/fiber.cpp'. Implement actual context switching.
    - OS Targets: Use 'CreateFiber'/'SwitchToFiber' for Windows, and 'makecontext'/'swapcontext' for POSIX.
    - Status: Complete. Real context switching implemented.
      • Windows: CreateFiber/SwitchToFiber/DeleteFiber with per-thread main fiber caching
      • POSIX: getcontext/makecontext/swapcontext with thread-local pointer passing
      • Each fiber gets dedicated 64KB stack
      • State machine: 0=ready → 1=running → 2=yielded → 3=done
      • yield() saves CPU registers and returns to scheduler; resume() restores context
      • Build verified: fiber.cpp compiles without warnings in aurora_runtime.lib

[x] TASK 1.2: Implement Autograd Backward Pass
    - Description: Neural network backward functions are currently exported but empty stubs.
    - AI Prompt Target: Build the computation graph engine in 'runtime/autograd.cpp'.
    - Requirements: Implement 'aurora_tensor_backward' and 'aurora_tensor_sgd_step' using chain rule.
    - Status: Complete. Autograd engine implemented as standalone module.
      • Created aurora/src/runtime/ai/autograd.cpp with all backward ops (add, mul, matmul, relu, sigmoid, tanh, softmax, cross_entropy, conv2d, maxpool2d, batchnorm, dropout, attention)
      • Exposed backward_* functions and link_grad in tensor.hpp
      • Added aurora_tensor_zero_grad() API for resetting gradients between training steps
      • Topological sort via Kahn's algorithm for correct backward traversal
      • SGD optimizer step (weight = weight - lr * grad)
      • Removed duplicate autograd code from tensor.cpp
      • Added autograd.cpp to CMakeLists.txt; build verified (aurora_runtime.lib compiles cleanly)

[x] TASK 1.3: Add Import Cycle Detection
    - Description: Circular imports currently cause compiler stack overflow.
    - AI Prompt Target: Modify 'compiler/import_resolver.cpp'.
    - Requirements: Implement a 'visited_paths' set or DFS graph tracking during module resolution to throw clear compilation errors.
    - Status: Complete. Cycle detection implemented in both code paths.
      • main.cpp: Refactored resolve_imports into resolve_imports_impl with std::vector (ordered chain) + std::unordered_set (fast lookup) tracking
      • On cycle: throws std::runtime_error with full resolution chain (A → B → C → A)
      • module_resolver.cpp: Added resolving_ member (std::unordered_set) to ModuleResolver
      • On cycle: prints clear error and returns nullptr
      • Both use fs::absolute() normalized paths to handle different path representations
      • Build verified: aurorac.exe compiles without errors

[x] TASK 1.4: Fix Type Checker Silent Unknowns
    - Description: Many 'infer_*' methods return 'Unknown' silently without enforcing safety.
    - AI Prompt Target: Update 'compiler/type_checker.cpp'.
    - Requirements: Strict type enforcement, eliminate silent 'Unknown' propagation, and add proper diagnostic error logging.
    - Status: Complete. Multiple fixes applied to eliminate silent Unknown propagation.
      • Added ast_kind_to_type() reverse mapping from AstTypeKind → AuroraType
      • Index expr (arr[i]): now returns element type from variable annotation instead of Unknown
      • Attribute expr (obj.field): now looks up field type from class registry via oop_class_of_var
      • Added oop_class_of_var() getter for object type map
      • expect_assignable: now emits diagnostic warning when Unknown propagates through a typed context
      • infer_unary default: now throws TypeError instead of silently returning Unknown
      • infer_call method calls: now looks up method return_kind from class registry
      • Build verified: aurorac.exe compiles cleanly

[x] TASK 1.5: Implement Parser Error Recovery
    - Description: A single syntax error aborts the entire compilation immediately.
    - AI Prompt Target: Modify 'compiler/parser.cpp'.
    - Requirements: Add parser synchronization points (e.g., sync to the next statement/semicolon) upon encountering an error.
    - Status: Complete. Parser now recovers from syntax errors and continues finding more.
      • Added std::vector<std::string> errors_ + bool had_error_ members to Parser class
      • Added public had_error() / errors() accessors for callers
      • parse_block(): wraps parse_stmt() in try-catch; on error records the message and advance()s past the bad line
      • parse_brace_block(): same try-catch recovery, with proper brace_depth increment/decrement
      • main.cpp: all three parser call sites (main compile, REPL, import resolver) now check parser.had_error() and print collected errors
      • Build verified: aurorac.exe compiles in Release mode

[x] TASK 1.6: Resolve Tensor API Redundancy
    - Description: Dual tensor APIs ('tensor_v1' and 'tensor_v2') cause redefinition errors.
    - AI Prompt Target: Clean up 'runtime/tensor/'.
    - Requirements: Deprecate or merge 'tensor_v1' into 'tensor_v2' to provide a unified API surface.
    - Status: Complete. Merged v2 into v1 for a unified API surface.
      • Added aurora_tensor_new_with_dtype() — unified constructor to v1 (dispatches by dtype)
      • Added aurora_tensor_gelu() + aurora_tensor_silu() to v1 (the only unique v2 features)
      • Updated dataloader.cpp to use v1 API instead of v2 (AuroraTensor, aurora_tensor_new_with_dtype, aurora_tensor_free)
      • Removed tensor_v2.cpp from CMakeLists.txt build
      • Rewrote tensor_v2.hpp as deprecated compatibility shim with #pragma message, typedef alias, and inline forwarders
      • Build verified: aurora_runtime.lib + aurorac.exe compile cleanly

[x] TASK 1.7: Fix Event Bus Vector Desynchronization
    - Description: Potential runtime crash on 'event_off' due to index mismatches.
    - AI Prompt Target: Update 'runtime/event_bus.cpp'.
    - Requirements: Change the internal storage mechanism to a vector of pairs containing handler and userdata.
    - Status: Complete. Merged parallel vectors into a single vector of pairs.
      • Replaced two parallel maps (handlers + user_data) with one map of std::vector<std::pair<handler, userdata>>
      • event_on: emplace_back() pushes a single pair — can never desync
      • event_off: erases from one vector; no second vector to go out of sync
      • event_emit: takes one snapshot of pairs; passes correct userdata to each handler
      • Added explicit #include <mutex> and #include <utility>
      • Build verified: aurora_runtime.lib + aurorac.exe compile cleanly

[x] TASK 1.8: Fix GC Internal Allocation
    - Description: Mark-sweep uses 'new std::vector' internally, risking self-collection.
    - AI Prompt Target: Update 'runtime/gc.cpp'.
    - Requirements: Avoid heap-allocating management structures inside the GC allocator's core loop.
    - Status: Complete. Replaced all dynamically-allocated management vectors with statics.
      • gc_objects: changed from `static std::vector<GCRecord>*` (heap via `new`) to `static std::vector<GCRecord>` (BSS/data segment)
      • gc_unknown_roots: same change; removed all `new std::vector<...>()` calls
      • arena_gc_roots: same change
      • Updated all 7 API functions (init, alloc, collect, register/unregister root, scan, clear) to use `.` syntax instead of `->`
      • Removed all null-guard checks on vectors (static vectors are always valid)
      • Build verified: aurora_runtime.lib + aurorac.exe compile cleanly


----------------------------------------------------------------------
PHASE 2: COMPILER MATURITY & ECOSYSTEM BRIDGES (PRIORITY 2)
----------------------------------------------------------------------
[x] TASK 2.1: Add DWARF Debug Info Generation
    - Description: No source-level debugging is possible currently.
    - AI Prompt Target: Implement 'compiler/codegen_debug.cpp' using LLVM DIBuilder.
    - Requirements: Generate proper line tables, variable scopes, and source file mappings.
    - Status: Complete. Integrated LLVM DIBuilder into codegen pipeline.
      • Added llvm::DIBuilder to LLVMCodegen (owned) + Codegen (non-owning pointer)
      • Compile unit created in Codegen::init_debug_info() with source file and directory
      • DISubprogram created per function (gen_function) and for entry/main wrappers
      • Line tables via DILocation set in gen_block() and gen_expr() using node->src_line
      • Variable scopes via dbg.declare in create_entry_alloca() for non-underscore vars
      • DIBasicType helper for int, float, string, pointer types
      • dibuilder_->finalize() called after all codegen in generate()
      • compile_aura_to_object() passes source path to enable debug info automatically
      • Build verified: aurora_runtime.lib + aurorac.exe compile cleanly

[x] TASK 2.2: Implement Generics/Templates Architecture
    - Description: Language lacks generic structures, forcing duplicate code for different types.
    - AI Prompt Target: Update 'compiler/parser.cpp' and 'compiler/type_checker.cpp'.
    - Requirements: Parse generic parameters '<T>' and handle compile-time template instantiation/monomorphization.
    - Status: Complete. Parsing, type-checker monomorphization, and codegen all implemented.
      • Parser: [T,U] generic params on function (after name, before '('), struct, class declarations; param type annotations (: TypeName); generic call syntax foo[Type1,Type2](args)
      • Type Checker: register_functions()/register_struct() detect generic params; infer_call() computes mangled name (foo__Int) and registers concrete FunctionTypeInfo
      • Codegen: No changes needed — all types are i64 at runtime; generic functions/calls use original names in LLVM IR
      • Commit: 2770527 (9 files, +204/-2)

[x] TASK 2.3: Complete Cross-Ecosystem Interop (Remove Stubs)
    - Description: Interop headers generate placeholder outputs ('/* call thunk */').
    - AI Prompt Target: Rewrite binding generators in 'compiler/bridge/'.
    - Requirements: Connect Python C API, QuickJS runtime, and Rust cdylib with functional FFI data marshaling.
    - Status: Complete. All three ecosystems already have functional FFI bridges.
      • Python C API: ffi.cpp (aurora_py_ensure_initialized + aurora_py_get_api) + bridge_main.cpp generates C code using dynamic python3.dll loading
      • QuickJS/NPM: bridge_npm.cpp generates C code embedding QuickJS (deps/quickjs/); test_quickjs_bridge.c confirms functionality
      • Rust/Cargo: bridge_cargo_gen.cpp generates Rust cdylib with serde_json marshaling; auto mode produces real wrappers
      • RAIIGuardGenerator stubs in raii_guard_gen.hpp are a code generation template (unused — no callers)
      • UniversalBindingGenerator produces .au binding declarations with Type IR cost annotations
      • EcosystemTypeIRMapper provides type mapping for all 4 ecosystems (PyPI, Npm, Cargo, Native)

[x] TASK 2.4: Implement Incremental Compilation
    - Description: Every execution re-parses all imports, leading to linear scaling bottlenecks.
    - AI Prompt Target: Update 'compiler/driver.cpp' and 'compiler/voss_pm/'.
    - Requirements: Create a build cache system that tracks file hashes and skips unchanged '.auf'/'.aura' files.
    - Status: Complete. Build cache system with --incremental flag.
      • Created include/compiler/build_cache.hpp — BuildCache class with SHA-256 hash tracking, file modification time + size comparisons
      • Modified main.cpp: added --incremental flag; cache check after import resolution (skips codegen/emit/link if unchanged); cache update on successful compilation
      • Cache stored in .aurora_cache/cache.txt as key-value entries

[ ] TASK 2.5: Fix Linker Search Paths (Windows)
    - Description: Builds fail on Windows if LLVM is not present in the system PATH environment variable.
    - AI Prompt Target: Update Windows-specific build driver logic.
    - Requirements: Add a fallback routine to search for MSVC 'link.exe' if LLVM's 'lld-link' is missing.


----------------------------------------------------------------------
PHASE 3: STANDARD LIBRARY & WEB PRODUCTION (PRIORITY 3)
----------------------------------------------------------------------
[ ] TASK 3.1: Expose JSON Serialization to User Code
    - Description: 'json.cpp' exists in the native runtime but lacks '.auf' bindings.
    - AI Prompt Target: Create 'stdlib/json.auf'.
    - Requirements: Expose native serialization and deserialization methods into standard library space.

[ ] TASK 3.2: Expand Core Standard Library Modules
    - Description: Missing vital modules for standalone real-world applications.
    - AI Prompt Target: Write '.auf' standard modules.
    - Requirements: Build missing libraries for networking sockets, filesystem traversals, datetime utilities, and regex parsing.

[ ] TASK 3.3: Implement TLS/SSL & WebSockets for HTTP Server
    - Description: The current HTTP server is raw socket-based, restricting production web use.
    - AI Prompt Target: Update 'runtime/http_server.cpp'.
    - Requirements: Integrate OpenSSL/MbedTLS for HTTPS and add a frame parser for WebSockets.

[ ] TASK 3.4: Add Match Statement Pattern Exhaustiveness Checking
    - Description: Match blocks can fail silently if a case is not covered.
    - AI Prompt Target: Update 'compiler/type_checker.cpp'.
    - Requirements: Implement algorithm to verify if all possible enum variants or type patterns are structurally covered.

[ ] TASK 3.5: REPL Indentation Wrapping & CLI QoL
    - Description: REPL crashes or generates malformed statements on multi-line indentation blocks.
    - AI Prompt Target: Update 'cli/repl.cpp'.
    - Requirements: Fix the 2-space indentation assumption, add an 'isatty' check for ANSI colors, and eliminate hardcoded escape sequences.