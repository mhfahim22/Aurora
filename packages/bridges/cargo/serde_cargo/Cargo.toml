[package]
name = "serde_bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = "1"
serde_json = "1"
serde = "1.0.228"

# ── Uncomment additional deps your wrapper needs:
# serde_bytes = "0.11"
# chrono = { version = "0.4", features = ["serde"] }
# uuid = { version = "1", features = ["serde"] }
