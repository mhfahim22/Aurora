[package]
name = "itertools_bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = "1"
serde_json = "1"
futures = "0.3"
itertools = { version = "0.14.0", features = ["use_alloc", "use_std"] }
