[package]
name = "rand_bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = "1"
serde_json = "1"
futures = "0.3"
rand = { version = "0.10.1", features = ["alloc", "chacha", "serde", "std", "std_rng", "sys_rng", "thread_rng"] }
