outputln("Testing PUT route alone...")
server app
    route "PUT" "/todos/:id"
        id = request.params.id
        body = request("body")
        json(aurora_todo_update(id, body, 0))
outputln("Server registered.")
