function map_arr(arr, fn)
    result = []
    for i in len(arr)
        result = push(result, fn(arr[i]))
    return result

nums = [1, 2, 3, 4, 5]
doubled = map_arr(nums, lambda(x) x * 2)
output("doubled[1] = " + doubled[1])
output("done")
