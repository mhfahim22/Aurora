# ── Zig cross-compiler (345 MB — download on demand) ──
tools/zig/

# ── Build Output ──
build/
build2/
_build/
build_asan/
build_aot/
build_release_no_gl/
output/
Testing/
crash/
CMakeFiles/
.aurora_cache/

# ── Rust target directories (2GB rebuildable artifacts) ──
packages/bridges/cargo/*/target/

# ── Node.js ──
node_modules/
packages/bridges/npm/*/node_modules/

# ── QuickJS build ──
quickjs/build/

# ── Compiled object files ──
*.obj
*.o
*.rlib
*.rmeta

# ── Windows DLL build artifacts ──
*.exp
*.lib
*.pdb
*.ilk
*.dll
!build/Release/*.exp

# ── Exes (keep only specific build outputs) ──
*.exe
!build/Release/*.exe

# ── Test/temp output files ──
*.txt
!CMakeLists.txt
!*.md
voss_cargo_*.txt
voss_lodash*.txt
voss_moment*.txt
test_out*.txt
test_err*.txt
test_std*.txt
test_min*.txt

# ── Test logs and artifacts ──
examples/*.log
examples/s1*
examples/msbuild_*
test_*.gz
SMOOTH_PLAN.md

# ── Additional build directories ──
build_release/

# ── Distribution / generated copies ──
dist/

# ── Demo / generated bindings output ──
demo_bindings_jvm/

# ── Ad-hoc root-level test files ──
/test_*.aura
/test_simple.aura

# ── Temp scripts ──
scripts/fix_c_casts.ps1

# ── Release build artifacts (generated by package_release.ps1) ──
release/*.zip

# ── Model/ML files ──
*.bin
*.onnx
*.mrc

# ── Generated PyPI stubs ──
*_pypi/

# ── Profiling artifacts (regenerated each run) ──
profile_results/

# ── Session / internal notes ──
SESSION_SUMMARY.md

# ── IDE / Editor ──
.vs/
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS ──
.DS_Store
Thumbs.db
nul
